const parser = require('@babel/parser'); const traverse = require('@babel/traverse').default; const fs = require('fs'); const code = fs.readFileSync('src/screens/Dashboard.js', 'utf8'); const ast = parser.parse(code, { sourceType: 'module', plugins: ['jsx'] }); traverse(ast, { JSXText(path) { if (path.node.value.trim().length > 0) { const parent = path.parent; if (parent.type === 'JSXFragment') { console.log('Stray text in Fragment line', path.node.loc.start.line, path.node.value.trim()); } } }, JSXExpressionContainer(path) { const parent = path.parent; if (parent.type === 'JSXFragment') { if(path.node.expression.type === 'LogicalExpression' && path.node.expression.operator === '&&') { console.log('Stray && in Fragment line', path.node.loc.start.line); } else if (path.node.expression.type === 'Identifier' || path.node.expression.type === 'MemberExpression') { console.log('Stray var in Fragment line', path.node.loc.start.line); } } } });
