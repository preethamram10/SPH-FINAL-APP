const parser = require('@babel/parser'); const traverse = require('@babel/traverse').default; const fs = require('fs'); const code = fs.readFileSync('src/screens/hr/HRDashboard.js', 'utf8'); const ast = parser.parse(code, { sourceType: 'module', plugins: ['jsx'] }); traverse(ast, { JSXExpressionContainer(path) { const parent = path.parent; if (parent.type === 'JSXElement' && !['Text', 'Avatar.Text', 'Chip', 'Button', 'Badge', 'List.Accordion', 'List.Item', 'Snackbar', 'Title', 'Paragraph'].includes(parent.openingElement.name.name)) { if(path.node.expression.type === 'LogicalExpression' && path.node.expression.operator === '&&') { const left = path.node.expression.left; if(left.type !== 'BinaryExpression' && left.type !== 'UnaryExpression' && left.type !== 'CallExpression') { console.log('Stray && in HRDashboard.js line', path.node.loc.start.line, 'Left type:', left.type); } } } } });
