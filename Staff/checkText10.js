const parser = require('@babel/parser'); const traverse = require('@babel/traverse').default; const fs = require('fs'); const code = fs.readFileSync('src/screens/Dashboard.js', 'utf8'); const ast = parser.parse(code, { sourceType: 'module', plugins: ['jsx'] }); traverse(ast, { JSXExpressionContainer(path) { const parent = path.parent; if (parent.type === 'JSXElement' && !['Text', 'Avatar.Text', 'Chip', 'Button', 'Badge', 'List.Accordion', 'List.Item'].includes(parent.openingElement.name.name)) { const type = path.node.expression.type; if (type !== 'JSXElement' && type !== 'JSXFragment' && type !== 'CallExpression' && type !== 'LogicalExpression' && type !== 'ConditionalExpression' && type !== 'ArrayExpression') console.log('Line', path.node.loc.start.line, 'Expression:', type, 'Source:', code.split('\n')[path.node.loc.start.line-1].trim()); } } });
