const parser = require('@babel/parser'); const traverse = require('@babel/traverse').default; const fs = require('fs'); const code = fs.readFileSync('src/screens/Dashboard.js', 'utf8'); const ast = parser.parse(code, { sourceType: 'module', plugins: ['jsx'] }); traverse(ast, { JSXExpressionContainer(path) { const parent = path.parent; if (parent.type === 'JSXElement' && !['Text', 'Avatar.Text', 'Chip', 'Button', 'Badge'].includes(parent.openingElement.name.name)) { if(path.node.expression.type === 'LogicalExpression' && path.node.expression.operator === '&&') { const left = path.node.expression.left; if(left.type === 'MemberExpression' && left.property.name === 'length') console.log('Length && at', path.node.loc.start.line); } else if (path.node.expression.type === 'ConditionalExpression') { const cons = path.node.expression.consequent; const alt = path.node.expression.alternate; if (cons.type === 'StringLiteral' || alt.type === 'StringLiteral') console.log('String ternary at', path.node.loc.start.line); } } } });
