const fs = require('fs'); const lines = fs.readFileSync('src/screens/hr/RevenueDashboard.js', 'utf8').split('\n'); lines.forEach((l, i) => { if(l.includes('&&') || l.includes('?') || l.includes('||')) { const trimmed = l.trim(); if(trimmed.startsWith('{') && trimmed.endsWith('}')) { if(trimmed.includes('&&') && !trimmed.includes('<Text') && !trimmed.includes('<View') && !trimmed.includes('<TouchableOpacity')) console.log('suspicious', i+1, trimmed); } } });
